---
variant: fcos
version: 1.5.0
storage:
#  filesystems:
#    - path: /
#      device: /dev/disk/by-label/ROOT
#      format: ext4
  files:
    - path: /etc/motd
      mode: 0644
      contents:
        inline: |
          ${message}
      overwrite: true

passwd:
  users:
    - name: ${username}
      password_hash:  "$y$j9T$hbiby344x5DWtMng5zf4SMMp$24xGfMZhMsbH6u/TB3FOZFtpGFrMa90i.OqpOOI0Cf2"
      ssh_authorized_keys:
        %{ for sshkey in sshkeys ~}
        - ssh-key ${trimspace(sshkey)}
        %{ endfor ~}